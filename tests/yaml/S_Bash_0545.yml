pipelines:
  - name: pipeline_S_Bash_0545
    steps:
      - name: S_Bash_0545
        type: Bash
        configuration:
          allowFailure: true
        execution:
          onExecute:
            - return 1
